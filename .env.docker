# Docker Compose 环境变量配置
# 复制此文件为 .env 并根据实际环境修改配置

# ================================
# 服务端口配置
# ================================
# 后端API服务端口（映射到宿主机的端口）
BACKEND_PORT=3000

# ================================
# 数据存储配置
# ================================
# 数据持久化目录（NAS上的绝对路径）
# 示例：/volume1/docker/fortune-draw/data
DATA_PATH=./data

# ================================
# 应用配置
# ================================
# 运行环境
NODE_ENV=production

# 数据库文件路径（容器内路径，通常不需要修改）
DB_PATH=/app/data/fortune.db

# ================================
# 安全配置
# ================================
# 允许的跨域来源（生产环境请修改为实际域名）
CORS_ORIGINS=https://servicewechat.com,https://your-domain.com

# ================================
# 监控配置
# ================================
# 健康检查间隔（秒）
HEALTH_CHECK_INTERVAL=30

# 日志级别
LOG_LEVEL=info

# ================================
# Cloudflare Tunnel 配置
# ================================
# Cloudflare Tunnel 名称
TUNNEL_NAME=fortune-draw-tunnel

# Cloudflare Tunnel Token（从Cloudflare Dashboard获取）
# TUNNEL_TOKEN=your-tunnel-token-here

# 内部服务URL（Tunnel指向的本地服务）
INTERNAL_SERVICE_URL=http://fortune-backend:3000